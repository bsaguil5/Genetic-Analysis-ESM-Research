# ESM2 Transporter Classifier

Protein classification using ESM2 for Sorghum bicolor transporters.

## Setup

```bash
python -m venv .venv
pip install -r requirements.txt
```

## Training

```bash
python classifier_model/esm_model/train.py
```

## Data

Training data: `data/READY_TO_TRAIN.csv`
